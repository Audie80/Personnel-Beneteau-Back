const chai = require('chai');
const assert = chai.assert;

// Import du fichier Javascript
let bddConnect = require('./bddConnect');

// Ensemble de tests
// describe('Test Suite', function () {
//     // 1er test : l'application est bien connectée à la BDD
//     describe('connection à la BDD', function () {
//         it('l\'application est bien connectée à la BDD', async function () {
//             let testObj = await bddConnect.bddConnect();
//             assert.equal(testObj, 'Success');
//         });
//     });

// });